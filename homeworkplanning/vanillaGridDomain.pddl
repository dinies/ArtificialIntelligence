(define (domain vanilla-grid)
  (:requirements :strips)
  (:predicates
    (conn ?square-1 ?square-2)
    (black ?square)
    (white ?square)
    (at ?who ?where )
    (place ?where ))
  (:action move
    :parameters (?who ?from ?to)
    :precondition (and (at ?who ?from)
                  (place ?from)
                  (place ?to)
                  (conn ?from ?to)
                  (not (black ?to)))
    :effect (and (at ?who ?to)
              (not( at ?who ?from)))
    )
)
